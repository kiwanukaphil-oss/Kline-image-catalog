# Browser-visible URLs only. Never add database, bucket or JWT secrets here.
NEXT_PUBLIC_POS_API_URL=http://127.0.0.1:5109/api
# Set this to the existing POS web app when reviewing contextual product links.
# NEXT_PUBLIC_POS_URL=https://your-pos-web-app.example
