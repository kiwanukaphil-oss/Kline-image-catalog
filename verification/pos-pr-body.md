The catalog workspace separates new-stock Receiving, shared Pricing with brand/size exceptions, and current POS Stock. Receiving uses a fixed reviewed revision, explicit size counts, atomic stock receipts and independent photo recovery. Existing-product restock requires explicit size matching and preserves POS prices/costs. Accidental intake is auditable and reversible, including queued upload IDs.

The normal POS server mounts the locked @kline/pos-workspace 0.16.0 package behind CATALOG_WORKSPACE_ENABLED. Migrations 106–108 add deliveries, photo-transfer tasks and cancellation records. The frontend handoff preserves product/branch context through login without credentials in URLs. Administration, item history and AI recovery retain permissions, stale-write protection and audit history.

Validation: the exact committed POS tree at 402133c passed a clean npm ci and all 249 backend tests across 26 suites. POS frontend passed 22 tests with one Vitest worker. Unrelated local AI edits are excluded from the branch and the clean release test. Catalog checks include 24 HTTP/database cases, eight clean axe views, a 1,000-lot/3,000-size workload, and a local database backup/restore with 13 matching table counts and 108 migrations. Latest isolated browser run passes 20/21; the stopped POS frontend prevents the final navigation repeat (the earlier same-version navigation check passed).

This is a draft release candidate. Staging needs to be selected or created; physical-device, real provider/private bucket and staff acceptance remain open. Production is unchanged, and this PR must not be merged as a substitute for those gates. The companion app and live checklist are at https://github.com/kiwanukaphil-oss/Kline-image-catalog.
